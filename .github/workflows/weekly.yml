name: weekly page check

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Run checker
        env:
          GMAIL_USER: ${{ secrets.MAIL_FROM }}
          TO_EMAIL:   ${{ secrets.MAIL_TO }}
          GMAIL_PASS: ${{ secrets.MAIL_PASS }}
        run: python check_shitennoji.py
